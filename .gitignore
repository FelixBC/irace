# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment and secrets (never commit real values; keep .env.example tracked)
.env
.env.*
!.env.example

# Local TLS / keys (if you generate certs for dev)
*.pem
*.p12
*.key

# Build outputs
dist/
build/
*.tsbuildinfo

# Vite / tooling caches
.cache/
.parcel-cache

# IDE and editors
.vscode/
.idea/
*.swp
*.swo
*~
# Cursor — ignore local workspace; keep shared rules under .cursor/rules/
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
desktop.ini

# Logs and runtime
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Tests and coverage
coverage/
*.lcov
.nyc_output/

# Optional caches
.eslintcache
.npm
*.tgz
.yarn-integrity
.node_repl_history

# Deployment / local overrides
.vercel/
.serverless/
docker-compose.override.yml

# Prisma — local SQLite experiments only (this project uses PostgreSQL in production)
*.db
*.db-journal

# Playwright MCP / local browser automation artifacts
.playwright-mcp/
playwright-report/
test-results/
blob-report/
